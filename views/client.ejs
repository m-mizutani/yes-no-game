<!DOCTYPE html>
<html>
  <head>
    <title>Happy Quiz</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
  
  <%= user.name %>さん
    <div id="">

      <% if (q !== undefined) { %>
      <h2><%= q.q %></h2>
      <% if (answer !== undefined && answer !== null) { %>
      <div><%= q.c[answer] %> で投票されました</div>
      <% } %>

      <ul>
      <% for(var i in q.c) { %>
      <li><a href="?a=<%= i %>"><%= q.c[i] %></a></li>
      <% } %>
      </ul>
      <% } %>
    </div>
  </form>
  </body>
</html>
